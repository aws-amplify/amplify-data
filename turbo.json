{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib-esm/**"]
    },
    "lint": {},
    "test": {
      "dependsOn": ["^test"]
    },
    "clean": {}
  }
}
