{
  "name": "@aws-amplify/amplify-api-next-types-alpha",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "types": "./lib-esm/index.d.ts",
  "typesVersions": {
    "<5": {
      "lib-esm/index.d.ts": [
        "index.v3.ts"
      ]
    }
  },
  "scripts": {
    "lint": "eslint \"**/*.ts*\"",
    "build:esm": "rimraf lib-esm && tsc -p ../../tsconfig.json --outDir lib-esm",
    "build:esm:watch": "rimraf lib-esm && tsc -p ../../tsconfig.json --outDir lib-esm --watch",
    "build": "npm run clean && npm run build:esm",
    "test": "echo \"No tests\"",
    "clean": "rimraf lib-esm"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
