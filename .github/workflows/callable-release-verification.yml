name: Release Verification Tests

on: workflow_call

jobs:
  prebuild-ubuntu:
    uses: ./.github/workflows/callable-prebuild-amplify-api-next.yml
    with:
      runs_on: ubuntu-latest
  # temp (for testing CI run):
  # prebuild-samples-staging:
  #   secrets: inherit
  #   uses: ./.github/workflows/callable-prebuild-samples-staging.yml
  # unit-tests:
  #   needs:
  #     - prebuild-ubuntu
  #   uses: ./.github/workflows/callable-unit-tests.yml
  # local-e2e:
  #   needs:
  #     - prebuild-ubuntu
  #   uses: ./.github/workflows/callable-local-e2e-tests.yml
  sandbox-e2e:
    needs:
      - prebuild-ubuntu
    uses: ./.github/workflows/callable-sandbox-e2e-tests.yml
  # temp (for testing CI run):
  # e2e:
  #   needs:
  #     - prebuild-ubuntu
  #     - prebuild-samples-staging
  #   secrets: inherit
  #   uses: ./.github/workflows/callable-e2e-tests.yml
